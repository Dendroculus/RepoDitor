# Compatibility

This directory contains data-only compatibility contracts shared across
RepoDitor products.

Product-specific runtime capability data remains within the owning product.

Current shared contracts:

- `es3/` — known compatibility vectors used to verify parity between the
  Desktop Python and Web Crypto ES3 implementations.
